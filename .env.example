# YummyAnime API token
VITE_APP_TOKEN=

# HCaptcha site key (used when the backend answers with a 420 captcha challenge).
# Note: hCaptcha does not work on the hostname `localhost` — open the dev server
# on http://127.0.0.1:5173 instead.
VITE_HCAPTCHA_SITE_KEY=b1847961-208e-4a90-9671-1e6bba9e0b36
